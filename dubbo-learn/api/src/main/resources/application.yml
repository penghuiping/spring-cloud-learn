###############################################
## common配置
###############################################
spring:
  profiles:
    active: development
  application:
    name: springbootlearn-api
server:
  port: 8081
  type: provider
###############################################
## dubbo配置
###############################################
dubbo:
  ## ApplicationConfig Bean
  application:
    id: dubbo-consumer-api
    name: dubbo-consumer-api
    qos-port: 22221
    qos-enable: true
    qosAcceptForeignIp: false
  ## RegistryConfig Bean
  registry:
    id: my-registry
    address: zookeeper://localhost:2181
    client: curator
###############################################
## apollo配置
###############################################
app:
  id: java_spring_boot_learn_api
apollo:
  bootstrap:
    enabled: true
  cacheDir: /data/apollo-cache
  meta: http://localhost:8080